<%= form_with(model: read) do |form| %>
  <% if read.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(read.errors.count, "error") %> prohibited this read from being saved:</h2>

      <ul>
        <% read.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :temperature %>
    <%= form.text_field :temperature %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
